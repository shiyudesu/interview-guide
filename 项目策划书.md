# InterviewGuide：面向真实岗位的自适应面试 Agent 平台

**项目策划书**

| 项目 | 内容 |
| --- | --- |
| 项目名称 | InterviewGuide（面向真实岗位的自适应面试 Agent 平台） |
| 参赛竞赛 | 东南大学 AI+ 创新应用大赛 |
| 参赛赛道 | AI Agent 应用创新 |
| 项目类型 | 可自托管的多租户 Web 应用（生产级完整系统，非原型演示） |
| 代码状态 | 已完成端到端实现，具备生产部署通道与持续集成门禁 |
| 团队信息 | 待补充（团队名称、成员分工、指导教师、院系） |
| 文档版本 | v1.0 |

---

## 一、项目摘要

InterviewGuide 是一个**可自托管、多租户、支持用户自带模型密钥（BYOK）的 AI 面试 Agent 平台**。它把"求职者准备面试"这一高频、高焦虑、低反馈效率的场景，重构为一条由 Agent 驱动的闭环：**简历结构化诊断 → 岗位画像匹配 → 自适应文字面试 → 实时语音面试 → 分组评分报告 → 私域知识库专项复盘 → 面试日程管理**。

与"把大模型包一层聊天框"的常见做法不同，本项目的核心是一个**带状态机约束的自适应面试 Agent**：候选人每提交一次回答，Agent 都会基于当前真实作答内容，在服务端强一致的事务边界内决定"继续追问"还是"进入下一主问题"，并由确定性规则对模型决策进行兜底与否决。模型只被允许输出 `FOLLOW_UP` 与 `NEXT_MAIN` 两个动作，会话终止 `COMPLETE` 只能由服务端状态机产生——这从架构上杜绝了"模型自说自话结束面试"的失控风险。

截至当前，项目已交付 **474 个受版本管理的文件、约 83,500 行源码**，其中后端 Python 约 35,700 行、前端 TypeScript/TSX 约 21,000 行；对外提供 **103 个后端接口（含 2 个 SSE 流式接口与 1 个 WebSocket 实时语音接口）**；后端 **194 个测试函数**（32 个单元测试文件 + 12 个真实基础设施集成测试文件），前端 8 个单元测试文件与 7 个 Playwright 浏览器用例；CI 包含 3 条工作流、7 个 Job 与变更感知门禁；生产交付具备 **多架构镜像 + GHCR 主动拉取 + 自动 HTTPS + 一键回滚** 的完整链路。

**一句话定位**：让每一个求职者都能拥有一位随叫随到、懂岗位、会追问、给得出可执行反馈的 AI 面试官。

---

## 二、项目背景与问题定义

### 2.1 行业背景

近年来国内高校毕业生规模持续处于历史高位，技术类岗位的招聘漏斗显著收窄，"简历投递—笔试—多轮技术面—HR 面"链条中，**技术面试是淘汰率最高、也是候选人最难自我训练的一环**。技术面试的本质不是知识默写，而是一场**由面试官主导的动态深挖对话**：候选人给出一个答案，面试官根据答案质量决定是就地追问边界条件，还是切换到下一个知识点。

与此同时，大模型能力已经足以承担"扮演面试官"的角色。真正的瓶颈从"模型能不能问"转移到了"**系统能不能可靠地、可控地、可复现地组织一场面试**"。

### 2.2 现有解决方案的三类缺口

我们调研并复现了当前市面上的主流做法，归纳出三类结构性缺口：

**缺口一：静态题库，缺乏追问能力。**
大量"AI 面试"产品实质是"题库 + 逐题打分"。系统在会话开始时一次性生成全部问题，候选人回答完 A 题就无条件跳到 B 题。这与真实面试的核心机制——**基于回答质量的动态深挖**——完全背离。候选人练到的是"背题"，而不是"扛住追问"。

**缺口二：会话不可靠，工程上不可用。**
一旦引入"边回答边决策"，就必然面对分布式系统的经典难题：网络重试导致的重复提交、模型超时、并发写入同一会话、异步评估任务丢失、进程崩溃后会话卡死。多数原型在这一层直接崩溃——重复提交产生两条追问、刷新页面丢失进度、评估任务永远处于"生成中"。

**缺口三：数据与密钥不可控，机构无法采用。**
简历是高敏感个人数据，企业内训资料是商业机密。SaaS 型产品要求用户把简历上传到第三方服务器，并把面试内容交给平台统一调用的模型账号。这使得**高校就业指导中心、企业内部培训部门这两类最有付费意愿的机构用户，在合规评审阶段就被卡死**。

### 2.3 问题定义

综上，本项目要解决的核心问题是：

> 如何构建一个**在真实分布式环境下可靠运行**的自适应面试 Agent，使其既能复现真实面试的动态追问机制，又能满足个人与机构对**数据主权、密钥自持、私有部署**的硬性要求？

---

## 三、目标用户与核心场景

### 3.1 用户画像

| 用户层 | 典型对象 | 核心诉求 | 付费意愿 |
| --- | --- | --- | --- |
| C 端个人 | 计算机相关专业本硕应届生、1–3 年经验的社招跳槽者 | 低成本、无社交压力地反复练习；获得可执行的改进项 | 中（对单次通过率敏感） |
| B 端机构（高校） | 高校就业指导中心、学院实验班、ACM/竞赛集训队 | 批量为学生提供面试训练；数据不出校；可对接本校课程资料 | 高（有专项经费与合规预算） |
| B 端机构（企业） | 企业内部培训部门、技术团队 Leader、外包/猎头机构 | 用内部技术文档生成专项面试；新人技能盘点；面试官培训 | 高（数据敏感度极高） |

### 3.2 核心场景

**场景一｜应届生冲刺大厂后端。** 上传简历 → 系统结构化分析并给出六维评分与改进建议 → 选择"Java 后端"岗位 Skill 或直接粘贴 JD → 进入文字面试，Agent 从简历项目切入，回答含糊时就地追问"你说的分布式锁，Redis 宕机时怎么保证不重复扣款"→ 面试结束后按主问题分组给出评分、参考答案与关键点差距。

**场景二｜临场语音模拟。** 选择 30 分钟语音面试，系统按"每 5 分钟一道主问题"规划节奏，并按 技术 50% / 项目 30% / HR 20% 的配比分配额度。候选人用麦克风作答，实时字幕同步显示，AI 面试官以合成语音追问。中途可暂停、可断线重连，会后异步生成完整评估报告。

**场景三｜机构私域知识库专项面试。** 高校教师上传本校《分布式系统》课程讲义与实验手册 → 系统完成解析、清洗、切片、向量化 → 一键从知识库生成带参考答案、关键点与评分量规（rubric）的题库 → 学生进入专项面试，所有问题与评分标准都锚定在本校教材上，杜绝模型泛泛而谈。

**场景四｜面试日程管理。** 候选人把 HR 发来的面试邀约原文粘贴进系统，Agent 自动解析出公司、岗位、轮次、时间与会议链接，生成日程条目并支持日/周/月视图与状态流转，过期日程由 Scheduler 自动归档。

---

## 四、产品方案

### 4.1 功能地图

平台由六大模块构成，前端共 **22 个页面、33 个复用组件**：

**① 简历中心。** 支持 PDF / DOC / DOCX / RTF 上传（单文件上限 10 MiB）。后端使用 pypdf 校验加密与页结构、pdfminer-six 提取文本、python-docx 解析段落与表格单元格；`.doc` 与 `.rtf` 通过镜像内置的无界面 LibreOffice 转换为 DOCX。解析结果经清洗后由 Agent 输出结构化分析：总分 + 内容 / 结构 / 技能 / 表达 / 项目 五个维度分项、优势列表、改进建议、摘要，并可导出 PDF 报告。

**② 岗位 Skill 与 JD 解析。** 内置 **13 个岗位 Skill 包**（Java 后端、Python 后端、Go 后端、前端、数据工程、DevOps/SRE、系统设计、算法、测试开发、AI Agent 开发，以及阿里 / 字节 / 腾讯风格的专项包），共 **66 个 Skill 资源文件**。用户也可直接粘贴 JD 原文，由 Agent 解析为可用于出题的岗位画像。

**③ 自适应面试（文字 / 知识库 / 语音三渠道统一）。** 三种渠道共用同一套 `InterviewTurnService`、同一套幂等规则与同一套评估服务，详见第五章。

**④ 知识库与 RAG。** 支持知识文档上传（单文件上限 50 MiB），异步完成切片与向量化；提供多知识库联合 RAG 对话（流式返回、Markdown 渲染、代码高亮、会话置顶/重命名/删除）与知识库出题。

**⑤ 面试日程。** 自然语言邀约解析、日/周/月/列表四视图、状态流转、过期自动归档。

**⑥ 设置与账号。** 每个账号独立管理 OpenAI 兼容 Provider、API Key、默认对话模型、默认 Embedding 模型与 ASR/TTS 语音配置；账号页支持改密与吊销全部会话。

**OpenTrek 校园赛模式。** 面向学校内网比赛，系统将简历、JD、文字面试、动态追问、评估和知识库能力固定路由到四个比赛专用 OpenTrek Agent。13 个岗位 Skill 经平台安全扫描后按请求动态绑定；预置资料进入 Kortex 文档知识库，本地只保留用户级影子记录和 SHA-256 映射。比赛页面隐藏 Provider、语音和知识库写入口，平台失败时明确报错，不转用其他生成模型。

### 4.2 差异化产品决策

- **不做 SaaS 独占，优先做自托管。** 一条 `./scripts/start.sh` 命令即可拉起完整生产 Compose；机构可在自有服务器完成部署，数据与密钥全程不出内网。
- **不内置平台密钥，强制 BYOK。** 系统内置的百炼 Provider **初始不带任何 API Key**。没有可用 Key 时，依赖模型的功能明确失败并引导用户前往设置页，**绝不回退到管理员、平台或其他用户的 Provider**。这既是安全边界，也让平台方彻底规避了模型调用成本与代扣风险。
- **不追求"全自动"，坚持人在环。** 知识库生成的题目先落为 DRAFT 状态，由使用者审核后再启用；这对机构用户的教学质量把控至关重要。

---

## 五、技术方案

### 5.1 总体架构

系统采用"**单镜像多进程 + 业务模块垂直切片**"的架构。生产环境由同一个 Python 镜像启动四类职责分离的进程：

| 进程 | 职责 | 启动约束 |
| --- | --- | --- |
| `migrate` | 通过 Alembic 升级数据库 | 一次性任务，成功后其他进程才允许启动 |
| `app` | 提供 REST / SSE / WebSocket / OpenAPI / 健康检查 | 单 Uvicorn worker |
| `worker` | 顺序消费四组 Redis Stream | 四个消费者并发 TaskGroup |
| `scheduler` | 恢复租约过期任务、清理过期资源 | 单实例，`max_instances=1` |

技术选型：

- **后端**：Python 3.13.13 + FastAPI + Pydantic + SQLAlchemy（异步）+ Alembic，依赖由 `uv 0.11.14` 锁定，提交 `uv.lock` 保证可复现构建。
- **数据层**：PostgreSQL 16 + pgvector 0.8.1（向量维度 1024，HNSW + cosine 索引）；Redis 7.4.2（Stream 任务队列、Session、幂等锁、限流、Provider 配置版本广播）；MinIO 等 S3 兼容对象存储（私有 Bucket）。
- **AI 层**：统一 OpenAI 兼容 LLM Adapter + LangGraph 出题状态图 + 结构化输出校验器；默认模型为 `qwen3.7-max`（对话）、`qwen3.7-text-embedding`（1024 维）、`qwen3-asr-flash-realtime`（识别）、`qwen3-tts-flash-realtime`（合成）。
- **前端**：React 18.3 + TypeScript 5.6 + Vite 5.4 + Tailwind CSS 4.1 + React Router 7；图表使用 Recharts，长列表使用 React Virtuoso 虚拟化，动效使用 Framer Motion。**未引入任何重型组件库**，全部 UI 自研以控制包体与视觉一致性。
- **交付层**：Docker Compose（生产 10 个服务）+ Caddy 自动 HTTPS 网关 + GHCR 多架构镜像 + systemd 定时主动拉取更新。

代码分层严格约束：`api.py` 只负责参数、校验、依赖注入与响应；`service.py` 负责业务编排；`repository.py` 负责数据访问并**在 SQL 层强制写入所有权条件**；API 使用 Pydantic DTO，数据库使用 SQLAlchemy ORM，**不直接返回 ORM 对象**。

### 5.2 核心创新：统一自适应面试 Agent

这是整个项目的技术内核，也是"AI Agent 应用创新"的直接体现。

#### 5.2.1 三渠道统一的 Turn 模型

文字面试、知识库专项面试、语音面试三条渠道，历史上通常各写一套逻辑。本项目将它们统一到同一个 Turn 状态机之上：

- 会话创建时**只生成或抽取主问题**，不预生成追问。
- 每提交一次回答，最多调用**一次**结构化模型决定下一步动作。
- 三渠道共用 `InterviewTurnService`、共用幂等规则、共用评估服务、共用 `interview:evaluate:stream` 评估队列。

数据结构上，`interview_questions` 表用 `kind`（MAIN / FOLLOW_UP）、`main_order`、`follow_up_order`、`parent_question_id` 自引用复合外键，把"主问题—追问"组织为一棵受数据库 CHECK 约束保护的树；`interview_turns` 表为每一次已提交回答保存 requestId、答案哈希、决策动作、置信度、Provider、Token 用量、耗时、处理租约与错误信息。

#### 5.2.2 决策流程：两段短事务 + 确定性规则前置

一次 Turn 提交的完整流程如下：

1. **短事务 1**：以 `SELECT ... FOR UPDATE` 锁定会话行，校验提交的 `questionId` 确为当前问题，插入一条 `PROCESSING` 状态的 Turn 并设置处理租约（lease）到期时间。**随即提交事务并释放锁**——绝不在持有行锁的状态下调用模型。
2. **确定性规则前置**：在调用模型之前，先执行一组零成本的确定性规则——空回答、显式"跳过 / 换题 / skip / pass"、已达到该主问题的追问上限、语音会话剩余时间预算不足。命中任一规则则直接进入下一主问题，**完全不消耗模型调用**。
3. **模型决策**：只有当"仍有可能追问"时才调用结构化模型。该调用显式设置 `max_attempts=1`、不调用 Tool、不启用任何隐式重试——因为这条链路直接决定用户端的感知延迟。
4. **服务端否决机制**：模型返回动作、追问文本、决策理由、话题标签与置信度。若**置信度低于 0.65**，或追问文本 / 话题经 Unicode 规范化后与该主问题下已有追问重复，则**强制否决模型决策，改为进入下一主问题**。
5. **短事务 2**：再次加行锁，落地决策——`FOLLOW_UP` 动态插入子题，`NEXT_MAIN` 选择已规划的下一主问题，无下一题则转 `COMPLETE`。
6. **会话完成**：在同一事务内写入 `evaluate_status=PENDING`，**事务提交后**才向 Redis 评估 Stream 投递消息，避免"消息已发但数据未落库"的经典竞态。

#### 5.2.3 三重幂等：Redis 快路径 + 数据库唯一约束 + 内容哈希校验

面试提交是典型的"用户会疯狂重试"场景（网络抖动、误触、页面刷新）。本项目采用三层防护：

- **第一层**：Redis 结果缓存与分布式锁，key 的作用域包含用户 UUID、会话 ID 与 requestId，实现快路径命中与并发抑制。
- **第二层**：数据库唯一约束作为最终一致性屏障——`UNIQUE(interview_session_id, request_id)` 防止同一请求重复处理，`UNIQUE(interview_session_id, question_id)` 防止同一道题被重复作答，`UNIQUE(user_id, request_id)` 防止会话重复创建。
- **第三层**：内容哈希校验。重放请求会校验 `questionId` 与答案的 SHA-256。**相同 requestId + 相同载荷 → 原样返回首次结果；相同 requestId + 不同载荷 → 明确返回业务错误**，而不是静默覆盖。

若发现已有 Turn 仍处于 `PROCESSING`，客户端会进入轮询等待；一旦租约过期，Scheduler 的 `adaptive-interview-recovery` 任务（30 秒周期）会回收该 Turn 并作确定性恢复，**不会二次调用决策模型**。

#### 5.2.4 上下文工程与 Prompt 注入防护

Agent 的上下文严格受限，只包含：当前主问题链、最近六轮对话、受限长度的简历/JD/Skill 摘要、以及题目参考资料快照。

所有进入 Prompt 的外部内容都经过 `PromptSanitizer` 处理：

- 检测并过滤伪造的 system / user / assistant 角色行；
- 过滤中英文的 "忽略以上指令 / ignore previous instructions / 你现在是" 等注入短语；
- 过滤伪造的文档分隔符与 `<data-boundary>` 标签；
- 用**带随机 ID 的边界标签**包裹用户数据，使模型可以区分"指令"与"数据"；
- 结构化输出的 system prompt 始终追加反注入指令。

此外，普通面试在**创建会话时**就把匹配到的 Skill reference 按分类保存为 `source_context` 快照，后续追问与评估**只读取快照，不再实时访问外部题库**。这既保证了同一场面试的评判标准前后一致，也彻底切断了生产链路对外部站点的运行时依赖。

#### 5.2.5 出题：LangGraph 状态图与三级降级

面试问题生成使用 LangGraph 编排为一张状态图：

```text
START → resolve_skill → allocate_question_counts → generate_with_fallback → merge_and_cap → END
```

当用户提供简历时，"基于简历项目出题"与"基于岗位方向出题"两条分支**并行执行**；任一分支失败时降级到方向题，方向题再失败时降级到静态题目。同时，对多个分类的 reference 采用**公平字符预算**分配，确保分类较多时后置分类不会因总长度上限被完全挤出上下文。

#### 5.2.6 评估：按主问题分组聚合

评估不是简单地"每道题打个分再平均"。系统按**主问题组**评分：一个主问题及其全部追问构成一组，追问的表现影响该组的技术深度、补充与纠错评价，但**不作为独立题目重复加权**。总分是所有已回答主问题组分数的等权平均，**未回答的计划问题不计入平均分**——避免候选人因提前结束而被不公平地拉低分数。

### 5.3 私域知识库与 RAG 管线

**解析与清洗。** 文本编码依次尝试 UTF-8 BOM、UTF-8、GB18030、Big5、Latin-1；清洗阶段移除控制字符、独占一行的图片文件名、图片与 `file:` URL、分隔线以及冗余空行。所有阻塞式解析均进入**带信号量限制的受控线程池**，不阻塞事件循环。

**切片。** 使用 tiktoken `cl100k_base` 分词器，默认 800 token 一块；非尾块优先在 `.` `?` `!` `\n` 处断开，但要求断点位置超过 350 字符以避免碎片化；长度不超过 5 字符的块不参与向量化；单文档最多 10,000 块。

**向量化的原子替换。** 这是一个容易被忽视但影响极大的工程细节：重新向量化时，新向量先以 `pending:{kb_id}:{job_id}` 的元数据写入；**在同一个事务内**完成"删除旧向量 → 提升新向量 → 更新块数与状态为 COMPLETED"。这保证了不会出现"旧向量已删、新向量未全"的半更新窗口，用户在重建索引期间的检索结果始终一致。

**检索。** 支持 LLM 查询改写（失败自动回退原问题），原问题与改写问题去重后依次检索。检索参数按查询长度自适应：≤4 字符时 top-k 20 / 最低相似度 0.18；≤12 字符时 top-k 12 / 0.28；长查询 top-k 8 / 0.28。相似度使用 pgvector 余弦距离 `1 - (embedding <=> query)`，**且在 SQL 内同时按知识库 ID 与 `user_id` 双重过滤**，而不是仅依赖向量元数据中的 KB ID。

**流式回答的安全兜底。** SSE 流式回答会先缓冲前 120 字符，检测"未检索到相关内容"一类的标记，命中后统一替换为安全的无结果文案，避免把模型的原始拒答措辞直接抛给用户。

**知识库出题。** 异步 Stream 任务，使用 4 个固定检索查询、每个 top-k 4，去重后汇总最多 12 个片段、上限 5,000 字符作为上下文；Prompt 中携带已有分类与近期题目以降低重复。结构化输出包含分类、题干、候选追问、参考答案、关键点、评分量规与话题摘要，并在同批次内按 Unicode 规范化后的题干做去重。生成结果先落为 DRAFT，并记录知识库内容哈希，用于后续识别"源文档已变更、题目可能过时（STALE）"。

### 5.4 实时语音面试链路

语音面试是延迟最敏感的链路，端到端设计如下：

**采集端（浏览器）。** `getUserMedia` 请求回声消除、噪声抑制与自动增益，目标采样率 16 kHz；使用 `AudioWorklet` 在音频线程完成重采样与 Float32→Int16 PCM 转换，每 **3,200 采样点（200 ms）** 输出一个块，Base64 编码后经 WebSocket 以 JSON 消息发送；同时用 `AnalyserNode` 每 100 ms 更新音量指示；配合 `@ricky0123/vad-web` 做浏览器端语音活动检测。AI 说话或正在提交轮次时**主动抑制上行音频**，避免自问自答。

**服务端。** 后端在 `accept` 连接**之前**就完成 Session、Origin 与资源所有权校验；随后作为客户端连接上游 Realtime ASR WebSocket。ASR 的 final 文本**只更新字幕**，真正的 Turn 提交仍由显式的 `control/submit` 消息触发——这把"识别正确性"与"作答意图"解耦，候选人可以在提交前编辑识别结果。ASR append 失败时自动重启会话，最多 15 次、间隔 80 ms；ready 监控最多重启 2 次。

**合成与回放。** TTS 按句并发合成（每会话默认最多 3 路并发，单句超时 8 秒），但**严格按顺序**向前端输出 `audio_chunk`。前端将增量 WAV 块 Base64 解码、跳过 44 字节 WAV 头、Int16→Float32 转换后进入 24 kHz 的 `AudioBuffer` 队列顺序播放，并配有播放完成检测与看门狗逻辑，防止 UI 永久卡在"AI 正在说话"状态。

**生命周期。** 计划时长限定 15–60 分钟且为 5 的倍数，每 5 分钟规划一道主问题；剩余额度按 技术 50% / 项目 30% / HR 20% 分配，自我介绍阶段启用时固定至少一题。**断线不自动结束会话**，客户端最多重连 3 次、间隔 2 秒；显式暂停与 Scheduler 的 `voice-interview-recovery` 任务共同负责生命周期清理。

### 5.5 异步任务与可靠性

四类重型任务通过 Redis Stream 解耦：

| 任务 | Stream | 消费组 |
| --- | --- | --- |
| 知识库向量化 | `knowledgebase:vectorize:stream` | `vectorize-group` |
| 简历分析 | `resume:analyze:stream` | `analyze-group` |
| 面试评估（文字与语音统一） | `interview:evaluate:stream` | `evaluate-group` |
| 知识库出题 | `knowledgebase:question-gen:stream` | `question-gen-group` |

消费逻辑遵循严格的顺序契约：**每轮先执行 `XAUTOCLAIM` 回收空闲超过 5 分钟的 pending 消息，无可回收消息时才执行 `XREADGROUP >` 读取新消息**。处理顺序固定为 解析 → 跳过判定 → 标记 PROCESSING → 处理 → 标记完成 → **最后 ACK**。

失败处理同样严格：业务失败且 `retryCount < 3` 时，**先 `XADD` 重投一条 retryCount+1 的新消息，再 ACK 旧消息**（最多 4 次业务尝试）；若重投方法本身抛出异常，则**不 ACK**，把消息留在 pending 中等待 `XAUTOCLAIM` 回收；重试耗尽则先写入业务表的 FAILED 状态与错误信息，再 ACK。整条链路不存在"静默吞掉异常"或"伪装成功"的路径。

Scheduler 提供第二道防线，四个恢复任务分别负责：面试日程过期归档（每小时）、知识库出题任务恢复（60 秒）、语音会话清理（60 秒）、自适应面试轮次与遗漏评估恢复（30 秒）。

### 5.6 多租户、BYOK 与安全体系

这是让本项目具备**机构级可采用性**的关键，也是工程量最大的部分之一（对应 4 次 Alembic 迁移中的 3 次）。

**认证与会话。** 密码使用 **Argon2id**（内存 65,536 KiB、时间成本 3、并行度 4、哈希长度 32、盐长 16）。Session Token 由 `secrets.token_urlsafe(32)` 生成，**Redis 中只存储其 SHA-256 摘要**作为 key，即使 Redis 被读取也无法反推出可用 Cookie。浏览器只保存 `Secure + HttpOnly + SameSite=Lax` 的 Session Cookie，**不持久化 Session Token、CSRF Token 或任何 Provider API Key**。Session 同时受空闲过期（默认 24 小时）与绝对过期（默认 7 天）双重限制。所有非 GET/HEAD/OPTIONS 的受保护请求必须同时通过 **CSRF Token 常量时间比对** 与 **Origin 白名单校验**。账户不存在时仍会验证一个 dummy hash，以消除账户枚举的时序差异。

**授权。** 授权由 **Repository 层的 SQL 条件强制执行**，而不是依赖前端过滤或 Service 层的 if 判断。跨用户的读取、修改、下载、关联与删除**统一按"资源不存在"处理**，不泄露目标资源是否存在。子资源可通过根资源继承所有权，但跨聚合引用（如面试引用的简历、RAG 会话引用的知识库、语音会话关联的核心会话）必须在写入前验证所有者一致。

**API Key 加密与 AAD 绑定。** 用户的 Provider API Key 使用 **AES-GCM（256 位密钥、12 字节 nonce）** 加密，且**将用户 UUID、Provider UUID 与加密版本号一并纳入 AAD**：

```text
provider-key:v{version}:{user_id}:{provider_id}
```

这意味着即使攻击者拿到数据库写权限，把 A 用户的密文行拷贝到 B 用户的记录上，**解密也会因 AAD 不匹配而直接失败**。主密钥保存在独立的 `provider_key` Docker 卷中（文件权限 0600），与业务数据库物理隔离。

**出站 SSRF 防护。** BYOK 意味着用户可以填写任意 Base URL，这是典型的 SSRF 攻击面。系统实现了 `ProviderOutboundPolicy`：默认仅允许 HTTPS/WSS；拒绝带凭据的 URL 与 fragment；拒绝重定向与环境代理；hostname 经 IDNA 规范化后解析**全部** IPv4/IPv6 地址，任一地址落入回环、私网、链路本地、保留段或云元数据地址即拒绝。更进一步，系统实现了**自定义 httpcore 网络后端**，在真正建立连接时再次解析并校验，然后直接连接到已校验的 IP——从而**防御 DNS rebinding 攻击**。可信内网 Provider 只能由部署者通过显式 allowlist 放行，普通用户无法自行放宽。

**限流。** 使用 Redis Lua 脚本实现原子的多维滑动窗口限流：GLOBAL / IP / USER 三个维度**先全部检查，任一失败则不修改任何维度计数，全部通过后才统一扣减**——避免了"部分维度已扣减但请求被拒"的计数污染。限流 key 使用业务 scope 而非类名。默认策略包括：登录 IP 20 次/分钟、账号 8 次/分钟；注册与验证邮件 5 次/小时；面试创建、轮次提交、JD 解析、知识库查询与出题均有独立限流。

**文件与对象存储。** 文件哈希的唯一性范围是**用户**而非全局：同一用户重复上传相同内容复用自己的资源，不同用户上传相同字节得到互相隔离的记录与对象。对象 key 包含用户命名空间，Bucket 保持私有，浏览器只能通过**经过所有权检查的下载接口**访问文件与报告。

**异步任务的 Provider 归属。** Worker 收到资源 ID 后**先从根资源解析所有者**，再取得该用户作用域的 Provider；缺失资源、禁用用户、不可用 Provider 或无效 Key 都写入明确的失败状态，**绝不使用全局或 legacy Provider 兜底**。任务创建时固定 Provider 选择，用户后续修改默认 Provider 不影响已排队任务；仍被未完成任务引用的 Provider 不允许删除。

**平滑迁移。** Alembic 迁移创建了一个不可登录且禁用的 `legacy-owner` 用户，用于承接升级前无法推断真实归属的存量资源；部署者创建首个管理员后，通过显式 CLI 将这些资源认领给目标账号。认领只更新所有者，不删除任何业务数据。

### 5.7 数据模型概览

当前共 **22 张表**，由 4 个 Alembic 迁移演进而来。核心分为四组：

- **用户与配置**：`users`、`user_password_credentials`、`user_llm_providers`（`UNIQUE(user_id, alias)`）、`user_ai_settings`、`user_voice_settings`。
- **简历与日程**：`resumes`（`UNIQUE(user_id, file_hash)`）、`resume_analyses`、`interview_schedule`。
- **面试**：`interview_sessions`、`interview_questions`、`interview_turns`、`voice_interview_sessions`（与核心会话一对一唯一外键）、`voice_interview_messages`。
- **知识库与 RAG**：`knowledge_bases`、`knowledge_base_questions`、`rag_chat_sessions`、`rag_chat_messages`、`rag_session_knowledge_bases`、`vector_store`（`vector(1024)` + HNSW 余弦索引）。

所有状态字段均由数据库 CHECK 约束保护，而不仅在应用层校验；`interview_questions` 的 MAIN/FOLLOW_UP 结构完整性同样由 CHECK 约束强制。

---

## 六、核心创新点总结

| # | 创新点 | 说明 | 对应赛道价值 |
| --- | --- | --- | --- |
| 1 | **模型决策 + 服务端否决的双层 Agent 控制** | 模型只能输出 FOLLOW_UP / NEXT_MAIN，`COMPLETE` 仅由状态机产生；置信度 < 0.65 或话题重复时服务端强制否决 | Agent 可控性的可复用范式 |
| 2 | **两段短事务 + 租约的 Turn 执行模型** | 绝不在持有数据库行锁时调用模型；租约保证崩溃后可恢复 | 解决 Agent 落地的高并发一致性难题 |
| 3 | **Redis 快路径 + 唯一约束 + 内容哈希的三重幂等** | 相同 requestId 不同载荷会明确报错而非静默覆盖 | Agent 应用的可靠性基线 |
| 4 | **三渠道统一 Turn 抽象** | 文字 / 知识库 / 语音共用同一状态机、幂等规则与评估服务 | 显著降低多模态 Agent 的维护熵 |
| 5 | **AAD 绑定用户身份的 BYOK 密钥体系 + DNS rebinding 级 SSRF 防护** | 密文跨记录搬移即失效；连接期二次校验 IP | 让 Agent 平台真正可被机构采用 |
| 6 | **数据边界标签 + 快照化参考资料的注入防护** | 随机 ID 边界标签隔离指令与数据；参考资料在建会话时快照冻结 | Agent 安全的工程化实践 |
| 7 | **向量索引的事务内原子替换** | 重建索引期间检索结果始终一致 | RAG 工程可用性 |
| 8 | **按主问题分组的评估聚合** | 追问影响深度评价但不重复加权；未答题不拉低均分 | 评价体系的公平性设计 |

---

## 七、工程实现与质量保障

### 7.1 项目规模

| 指标 | 数值 |
| --- | --- |
| 受版本管理文件 | 474 |
| 源码总行数（剔除锁文件与二进制字体） | 约 83,531 行 |
| Python（应用 + 迁移 + 测试） | 190 文件 / 35,724 行 |
| TypeScript + TSX | 111 文件 / 21,049 行 |
| 提交次数 | 119 |
| 后端接口 | 103 个（含 2 SSE + 1 WebSocket） |
| 数据库表 / Alembic 迁移 / PostgreSQL 扩展 | 22 / 4 / 3 |
| 岗位 Skill 包 / Skill 资源 / Prompt 模板 | 13 / 66 / 16 |
| 前端页面 / 复用组件 | 22 / 33 |

### 7.2 测试体系

- **后端 243 个测试函数**：37 个单元测试文件（222 个函数）覆盖 AI 资源与 Prompt、Skill、鉴权、数据库 Schema、文档解析、S3、PDF 导出、面试与 Turn 决策、日程、简历、知识库与 RAG、题库、向量化、LLM Adapter、OpenTrek Client/Kortex/provisioning/比赛模式、出站策略、Redis Stream 与限流、遥测、语音配置与 WebSocket 核心；12 个集成测试文件（21 个函数，其中 20 个使用 `integration` 门禁）**使用真实 PostgreSQL/pgvector、Redis 与 S3 兼容存储**，涵盖鉴权、面试、日程、知识库、题库面试、Provider Registry、RAG 对话、Redis、S3、**双租户隔离**、用户级 Provider 与向量化 Worker。
- **前端**：8 个单元测试文件 / 26 个用例（面试统计、出题状态、题目容量、面试进入与完成、语音评估、重连行为、WebSocket URL 解析）；7 个 Playwright 规格文件，其中含一个 **Pixel 5（360×800）移动端专项项目**，6 个移动端场景验证导航、卡片布局、日程、RAG 侧栏、设置弹窗与文档级横向溢出。
- **真实模型验收**：`real-model.yml` 工作流仅支持手动触发，运行在**受保护环境**中并使用受保护密钥，执行 5 次真实对话请求、2 次结构化轮次决策、1024 维 Embedding 校验、15 秒内 ASR WebSocket 就绪、非空 TTS PCM 输出，并上传**已脱敏**的 JSON 报告（即使失败也上传）。原则明确：**真实模型验收不允许用 fake 冒充**。

### 7.3 持续集成

CI 由 3 条工作流构成，主工作流 `ci.yml` 含 7 个 Job：

1. **变更检测**：`tools/scripts/detect_ci_changes.py` 按路径将变更分类为 8 个领域；工作流自身变更、分类脚本变更、空变更集与手动触发**强制全量运行**。
2. **仓库策略**：提交信息钩子测试、5 个工具测试文件、Markdown 链接与行尾空格检查、环境变量文档一致性检查、清单重生成比对、Shell/PowerShell 语法检查、生产/测试/部署 Compose 与 Caddy 配置校验。
3. **后端检查**：`ruff check` + `ruff format --check` + `mypy src`（strict）+ `pytest`，构建后端镜像，并**验证镜像中不含 Java**、验证无界面 RTF→DOCX 转换可用。
4. **前端检查**：ESLint + 单元测试 + Playwright + TypeScript/Vite 生产构建。
5. **模型代理检查**：验证 HTTP 与 WebSocket 代理行为。
6. **生产 Compose 集成**：构建并启动完整 Compose 并等待健康、创建隔离的集成数据库并迁移、运行 `pytest -m integration`、运行标记 `@real-backend` 的浏览器用例、**重建 API 容器后验证前端代理仍能动态解析到新容器**。
7. **CI gate**：要求仓库策略与全部被选中的 Job 均成功。

此外，仓库维护 6 份**确定性清单**（API、配置、数据库、Redis、资源、汇总），由脚本生成并在 CI 中比对。清单检查会**拒绝任何前端调用但后端不存在的接口**（当前 frontend-only 调用数为 0），从机制上消灭前后端契约漂移。

### 7.4 部署与交付

**本地/自托管**：`./scripts/start.sh`（Windows 双击 `start.cmd`）。脚本非破坏性设计——不覆盖已有 `.env`、不删除数据卷；自动生成基础设施随机密码（**不回退到仓库内置弱密码**）；在构建前检查宿主机端口占用并给出对应变量、建议端口与占用进程；校验架构（amd64/arm64）。生产 Compose **只发布前端入口且默认绑定 `127.0.0.1`**，API、PostgreSQL、Redis 与对象存储**不映射宿主机端口**。

**OpenTrek 校园赛部署**：`./scripts/start-campus.sh` 使用独立 `.env.campus`、Compose 项目名和数据卷，在 Ubuntu/Debian x86_64 校园主机上只发布前端 HTTP 入口。启动前由 provisioning CLI 幂等创建比赛 OpenTrek应用密钥、四个不可变 Agent 版本、13 个扫描通过的 Skill 和已完成向量化的 Kortex 知识库；启动脚本分别验证宿主机、API 容器和 Worker 容器到校内 OpenTrek 的连通性。该方式明确限定为校园网临时比赛环境，不宣称替代正式 HTTPS。

**服务器正式部署**：GHCR 主动拉取通道。只有 CI 在当前 `main` commit 成功后才允许推进；backend、frontend 先发布同一 revision 的**不可变 `sha-<commit>` 多架构镜像**（linux/amd64 + linux/arm64，附带 SBOM 与 provenance），部署包最后发布。服务器通过 systemd 定时器主动拉取（开机后 2 分钟首检、之后每 5 分钟、随机延迟 30 秒），**GitHub Actions 不持有服务器 SSH Key**。更新流程强制：校验镜像 OCI revision 标签与 SHA tag 匹配 → 先起基础设施 → 强制执行 Bucket 初始化与 Alembic 迁移 → 等待健康检查 → 通过本机 443 验证真实域名证书与 `/health` → 记录 `current-tag` 与 `previous-tag`。失败自动恢复，`rollback.sh` 可一键回滚到上一成功版本（明确声明**不回滚数据库迁移**）。

**HTTPS**：Caddy 作为唯一公网入口发布 80/443，通过 Let's Encrypt 自动签发与续期，80 端口仅用于 ACME 与跳转；证书状态保存在独立数据卷。安全响应头包含 HSTS、frame/content/referrer 策略与 `Permissions-Policy: microphone=(self)`。同时支持 `--external-caddy` 模式，与服务器已有的宿主机 Caddy 共存。

**镜像工程**：后端镜像固定 Python 3.13.13-slim-bookworm，以 **非 root UID/GID 10001** 运行；保留 LibreOffice 文档转换能力但使用 nogui 包，并把大型系统依赖与 Python site-packages **拆分为有界 layer**，避免弱网络环境下因单个超大 blob 无法完成拉取。

---

## 八、商业模式与市场分析

> 说明：以下市场判断基于公开行业趋势的定性分析与本项目已实现的成本结构推算。具体量化数据在正式提交前需补充可引用的权威来源。

### 8.1 市场机会

技术岗位求职培训市场存在明显的**两端分化**：一端是低价的题库/刷题产品（同质化严重、无追问能力），另一端是高价的真人模拟面试服务（单次成本高、供给受限于资深面试官人数、难以规模化）。**中间地带——"接近真人面试体验、但边际成本接近于零"——正是 AI Agent 的天然位置。**

同时，高校就业指导与企业内训这两个 B 端市场长期缺乏合适的工具：通用 SaaS 产品因数据合规无法进场，自研又缺乏工程能力。本项目"**可自托管 + BYOK + 私域知识库**"的组合恰好填补该空缺。

### 8.2 价值主张

| 用户 | 现状痛点 | 本项目提供的价值 |
| --- | --- | --- |
| 应届生 | 找不到人陪练；刷题无法应对追问 | 随时可用的动态追问面试官；分组评分与关键点差距报告 |
| 社招跳槽者 | 时间碎片化；需要针对具体 JD 准备 | 粘贴 JD 即出题；15–60 分钟语音面试可灵活安排 |
| 高校就业中心 | 一对一辅导供给严重不足；学生数据不能外传 | 校内自托管；用本校教材生成专项题库；批量覆盖 |
| 企业培训部门 | 内部技术文档无法喂给公有云 | 完全私有部署；BYOK 接自有模型网关；新人技能盘点 |

### 8.3 商业模式设计

采用**开源核心 + 商业增值**的三层结构：

**第一层｜开源自托管版（免费）。**
完整功能开源，用户自备服务器与模型 API Key。价值在于快速获取开发者与高校用户、建立技术口碑、形成社区贡献。**平台方不承担任何模型调用成本**——这是 BYOK 设计带来的直接商业优势。

**第二层｜托管 SaaS 版（订阅制）。**
面向不愿运维的个人用户，提供托管实例。用户仍可选择 BYOK（仅付托管费）或使用平台代充额度（托管费 + 用量费）。预期定价区间：个人版月付 29–49 元（BYOK 模式）；含模型额度的标准版 99–199 元/月。

**第三层｜机构授权版（年费 + 服务）。**
面向高校与企业，提供私有化部署授权、SSO 对接、批量账号管理、教师端班级看板、面试数据分析报表与技术支持。定价按账号规模阶梯计价，预期高校版 3–8 万元/年、企业版 8–30 万元/年，另有部署实施与定制 Skill 包开发的一次性服务费。

**延伸收入**：定制岗位 Skill 包（如金融科技、车载软件等垂直方向）、企业面试官培训服务、与招聘平台的能力 API 合作。

### 8.4 成本结构与单位经济

本项目的成本结构对创业阶段极其友好：

- **模型成本**：在 BYOK 模式下**完全由用户承担**，平台方为零。这从根本上规避了 AI 应用最典型的"用户越多亏损越大"陷阱。
- **基础设施成本**：托管版单实例可复用同一套 Compose，PostgreSQL/Redis/对象存储均为开源组件，无商业许可费用。
- **研发成本**：核心系统已完成，后续投入以功能迭代与 B 端定制为主。
- **交付成本**：GHCR 主动拉取 + systemd 自动更新的设计使得**私有化交付几乎零边际运维成本**——客户服务器自动拉取更新，无需逐个登录维护。

因此，机构授权版的毛利率可维持在很高水平，主要变动成本是实施与支持人力。

### 8.5 市场进入策略

**阶段一（0–6 个月）｜开发者与高校种子。** 以开源项目形式发布，在技术社区传播"自适应面试 Agent 的工程实现"这一技术叙事（本项目的分布式一致性与 Agent 可控性设计本身就是优质技术内容）。同步接洽 2–3 所高校就业指导中心做免费试点，换取真实使用数据与案例。

**阶段二（6–18 个月）｜B 端转化。** 基于试点案例形成标准化的高校解决方案（含教师端看板与班级报表），通过高校就业系统集成商渠道推广。同时上线托管 SaaS 版承接开源用户的付费转化。

**阶段三（18 个月以后）｜垂直深化。** 面向企业推出内训版，重点打"用企业自有技术文档生成面试"这一不可替代的场景；开放能力 API 与招聘/在线教育平台合作。

### 8.6 竞争分析

| 维度 | 静态题库类产品 | 通用大模型对话 | 真人模拟面试 | **InterviewGuide** |
| --- | --- | --- | --- | --- |
| 动态追问 | 无 | 有但不可控 | 有 | **有且服务端可控** |
| 岗位针对性 | 中 | 低 | 高 | **高（13 个 Skill 包 + JD 解析）** |
| 语音实时面试 | 少见 | 无 | 有 | **有（ASR/TTS 全双工）** |
| 私域知识库 | 无 | 无 | 不适用 | **有（RAG + 自动出题）** |
| 数据主权 | 无 | 无 | 不适用 | **完全自托管** |
| 密钥自持 | 不适用 | 无 | 不适用 | **强制 BYOK** |
| 会话可靠性 | 不适用 | 无保障 | 不适用 | **三重幂等 + 租约恢复** |
| 边际成本 | 低 | 低 | **极高** | 低（BYOK 下平台方为零） |

**核心护城河**不在于"用了大模型"，而在于**把一个 Agent 应用做到生产级可靠与机构级安全所需的工程纵深**——两段短事务的并发模型、三重幂等、Stream 重投与 ACK 顺序、AAD 绑定的密钥体系、DNS rebinding 级 SSRF 防护、确定性清单驱动的契约治理。这些能力无法通过调用几个 API 复现，构成了真实的复制门槛。

---

## 九、合规、伦理与风险

### 9.1 数据合规

简历属于高敏感个人信息。本项目的应对是**架构级**的而非条款级的：自托管模式下数据完全不出用户内网；托管模式下用户资源在 Repository 层强制按 `user_id` 隔离，跨用户访问统一按"资源不存在"处理；对象存储 Bucket 保持私有，仅通过所有权校验后的接口下载。

### 9.2 内容与版权合规

面试参考资料的来源治理已制度化：所有外部来源必须登记在 `tools/reference_sources/catalog.json`，标明许可证与**允许用途**（`adapt-with-attribution` 允许整理改写并署名 / `discovery-only` 仅用于发现方向不得复制 / `link-only` 仅保留标题与链接 / `verify-only` 仅作事实校验）。遇到许可证不明、非商业限制或禁止演绎的内容，默认降级为 `discovery-only` 或不使用。GitHub 内容源**固定到 40 位 commit SHA**，保证可追溯。**外部题库只参与离线采集，生产 API、Worker 与模型调用链不访问任何外部来源。**

### 9.3 AI 伦理

- **不承诺就业结果。** 产品定位是"训练工具"，评估报告明确标注为参考性反馈，不用于任何真实招聘决策。
- **人在环。** 知识库生成的题目先落 DRAFT，需人工审核后启用。
- **透明失败。** 模型不可用时明确失败并引导配置，不伪造结果、不静默降级到低质量输出。
- **注册默认关闭。** `APP_AUTH_REGISTRATION_ENABLED` 默认关闭，部署者必须完成真实 HTTPS、SMTP 到达率与双用户隔离现场验收后才可显式开放，避免不成熟的部署暴露在公网。

### 9.4 主要风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 上游模型 API 变更或不可用 | 核心功能中断 | 统一 OpenAI 兼容 Adapter，Provider 可热切换；模型列表自动发现；远端失败时明确标记为兜底而非伪装成功 |
| 模型输出不稳定导致体验波动 | 面试质量下降 | 结构化输出 + Pydantic 强校验 + 引号修复重解析；置信度阈值否决；三级降级到静态题目 |
| 用户模型成本超预期 | 用户流失 | 确定性规则前置，空答/跳过/达上限场景零模型调用；轮次决策 `max_attempts=1`；上下文严格截断 |
| 自托管带来的部署门槛 | B 端转化受阻 | 一键脚本 + 端口冲突诊断 + GHCR 自动更新 + systemd 定时器，把运维成本压到接近零 |
| 提示词注入与越权 | 安全事故 | PromptSanitizer + 数据边界标签 + Repository 层强制所有权 + AAD 密钥绑定 + 出站 SSRF 策略 |
| 竞品快速跟进 | 市场份额 | 护城河在工程纵深而非功能列表；持续以开源社区与机构案例建立先发优势 |

---

## 十、发展规划

**近期（0–6 个月）｜可信度建设**

- 完成正式域名 HTTPS、SMTP 与双用户隔离的服务器现场验收，开放自助注册。
- 扩充 Skill 包至 20 个以上，覆盖更多垂直方向。
- 引入检索重排序（当前为纯 pgvector 余弦排序），提升 RAG 精度。
- 补充可访问性自动化检测与 `prefers-reduced-motion` 支持。

**中期（6–18 个月）｜机构化能力**

- 教师端/管理端：班级管理、批量账号、面试数据看板与能力雷达对比。
- SSO 对接（CAS/OIDC），适配高校统一身份认证。
- 面试录像与多模态评估（表达流畅度、语速、停顿分析）。
- 国际化框架接入（当前为中文硬编码，已具备改造条件）。

**远期（18 个月以上）｜生态与平台化**

- 开放 Skill 包与 Prompt 模板的插件机制，形成社区贡献生态。
- 能力 API 开放，与招聘平台、在线教育平台形成互补合作。
- 基于脱敏聚合数据构建"岗位能力图谱"，反哺出题与评估质量。

---

## 十一、团队

> 本节待补充。建议填写：团队名称、成员姓名与院系年级、分工（架构 / 后端 / 前端 / AI 工程 / 产品与商业）、指导教师、既往相关经历与获奖情况。

---

## 十二、附录

### 附录 A：关键技术指标速查

| 类别 | 指标 |
| --- | --- |
| 后端语言/框架 | Python 3.13.13 / FastAPI / SQLAlchemy / Alembic，依赖由 uv 0.11.14 锁定 |
| 前端语言/框架 | TypeScript 5.6 / React 18.3 / Vite 5.4 / Tailwind CSS 4.1 |
| 数据库 | PostgreSQL 16 + pgvector 0.8.1，22 张表 / 4 次迁移 / 3 个扩展，vector(1024)，HNSW + cosine |
| 队列 | Redis 7.4.2 Stream，4 组消费组，XAUTOCLAIM 空闲阈值 5 分钟，最多 4 次业务尝试 |
| 接口 | 103 个后端接口（2 SSE + 1 WebSocket），前端调用 98 个，frontend-only 调用 0 个 |
| Agent 决策 | 置信度阈值 0.65，轮次决策 max_attempts=1，动作集 {FOLLOW_UP, NEXT_MAIN} |
| 语音 | 上行 16 kHz / 200 ms PCM 分块；下行 24 kHz 顺序播放；TTS 并发 3、单句超时 8 秒 |
| 安全 | Argon2id (64 MiB, t=3, p=4)；AES-GCM + 用户/Provider AAD 绑定；三维滑动窗口限流 |
| 测试 | 后端 194 个测试函数（含 12 个真实基础设施集成文件）；前端 26 单测用例 + 7 Playwright 规格 |
| 交付 | 生产 Compose 10 服务；GHCR 多架构（amd64/arm64）不可变镜像；Caddy 自动 HTTPS；一键回滚 |

### 附录 B：术语表

| 术语 | 说明 |
| --- | --- |
| Agent | 此处特指具备状态、可基于中间结果自主决策下一步动作的 LLM 应用体 |
| Turn | 一次"提交回答 → Agent 决策 → 产出下一问题"的完整轮次 |
| 主问题 / 追问 | MAIN 为预先规划的知识点问题；FOLLOW_UP 为基于真实回答动态生成的深挖问题 |
| BYOK | Bring Your Own Key，用户自带模型 API 密钥 |
| AAD | Additional Authenticated Data，AES-GCM 中参与认证但不加密的附加数据 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Rubric | 评分量规，题目对应的结构化评分标准 |
| 租约（Lease） | 为处理中的任务设置的过期时间，超时后由恢复任务回收 |
| 幂等 | 相同请求重复执行产生相同结果，不产生重复副作用 |
| SSRF | Server-Side Request Forgery，服务端请求伪造 |

### 附录 C：项目结构

```text
backend/                FastAPI 后端、Alembic 迁移、Skill/Prompt 资源与测试
frontend/               React 前端、Playwright 用例与 Nginx 配置
tools/                  确定性清单、参考资料治理、模型诊断代理与生产验收工具
scripts/                跨平台启动、停止与辅助脚本
docs/                   配置、运维、部署与架构文档
deploy/                 GHCR 部署包、主动更新、systemd 单元与回滚脚本
docker-compose.yml      生产 Compose（10 个服务）
```

---

*本策划书所述全部技术细节均对应仓库中已实现并经测试覆盖的代码，不含规划中但未实现的功能描述。*
